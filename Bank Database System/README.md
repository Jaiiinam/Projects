# Bank Database System 

```bash
- First, run Load.py, this will create the database, create the tables, and load the tables with some data. 
- Finally, run Modify.py, this will allow you to edit, view, or update data in the database with some pre-defined queries.
```
